                    <div class="col-lg-12">
                      <div class="panel panel-default">
                        <div class="panel-heading">
                            <h3 class="panel-title"><i class="fa fa-user fa-fw"></i>All Users</h3>
                        </div>
                        <div class="panel-body">
                          <div class="table-responsive">
                              <table class="table table-bordered table-hover table-striped">
                                  <thead>
                                      <tr>
                                          <th>Username</th>
                                          <th>Level</th>
                                          <th>Points</th>
                                          <th>Icon of Choice</th>
                                      </tr>
                                  </thead>
                                  <tbody>
                                    <% users = User.all.order(level: :desc) %>
                                    <% x=0 %>
                                    <% users.length.times do %>
                                      <tr>
                                        <% if users[x] == @user %>
                                          <td>YOU!!</td>
                                        <% else %>
                                          <td><%= users[x].username %></td>
                                        <% end %>
                                        <td><%= users[x].level %></td>
                                        <td><%= user_points =users[x].total_points(users[x]) %></td>
                                        <td><img src="<%= users[x].icon %>" height=25px></td>
                                      </tr>
                                      <% x+=1 %>
                                    <%end%>
                                  </tbody>
                              </table>
                          </div>
                          <div class="text-right">
                              <a href="">View All Users <i class="fa fa-arrow-circle-right"></i></a>
                          </div>
                        </div>
                      </div>
                    </div>